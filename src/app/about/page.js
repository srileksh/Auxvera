import AboutSection from '@/components/AboutSection'
import React from 'react'

export const metadata = {
  title: "About",
};

function page() {
  return (
    <div>
      <AboutSection/>
    </div>
  )
}

export default page